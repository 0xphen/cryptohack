[package]
name = "utilities"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
hex-to-base64 = { git = "https://github.com/0xphen/cryptopals.git", branch = "main" }
